-- typy lodi, ktere pokryvaji pouze pravidelne spoje

-- { {POKRYTI *> LOD}\{PSP *> LOD} }[ LTYP ]

-- lode, ktere nebyly rezervovany

-- LOD - { REZERVACE*>LOD }

-- dvojice zakazniku, kteri bydli na stejne adrese

-- {zakaznik[zid->zid1, adresa, jmenoz->jmeno1, prijmeni->prijmeni1]*zakaznik[zid->zid2, adresa, jmenoz->jmeno2, prijmeni->prijmeni2]}(zid1<zid2)
